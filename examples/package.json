{
  "scripts": {
    "foo": "touch foo.txt",
    "ls": "ls -la",
    "clean": "rm foo.txt",
    "me-pkg": "echo $_DS_CURRENT_FILE",
    "me-other": "ds -f pyproject.toml me-pyproject",
    "tests": "ds foo ls clean"
  }
}
